<div class="jumbotron">
  <h1 class="col-md-offset-2">Heading</h1>
</div>
  <h2 class="col-md-offset-2">What services are you looking for?</h2>
  <div id="category-buttons" class="col-md-offset-2">
    <p>
      <button type="button" id="plastic-injection" class="btn btn-success btn-lg">Plastic Injection Molding</button>
      <button type="button" id="laser-cutting" class="btn btn-info btn-lg">Laser/Water-jet Cutting</button>
      <button type="button" id="prototype" class="btn btn-warning btn-lg">Prototype Building</button>
      <button type="button" id= "3d-printing"class="btn btn-danger btn-lg">3d Printing</button>
      <button type="button" id="clear-filters" class="btn btn-default btn-lg"><span class="glyphicon glyphicon-remove"></span>Clear Filters</button>
    </p>
  </div>

    <div class="contacts container-fluid">
    {{#each EngineeringContacts}}
      <div class="each-contact panel panel-primary col-md-8 col-md-offset-2">
       <div class="panel-heading">
          <h2 class="panel-title">{{this.Name}}</h2>
          <span class="glyphicon glyphicon-edit pull-right">
          </span>
        </div>
        <div class="panel-body">
          <a href={{this.Website}}>{{this.Website}}</a><br>
          <a href="mailto:{{this.Email}}">{{this.Email}}</a>
          <p>{{this.Phone}}</p>
          <p>{{this.Address}}</p>
          <p>{{this.Tags}}</p>
        </div>
      </div>
    {{/each}}
  </div>
